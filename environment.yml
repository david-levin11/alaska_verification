name: alaska-verify
channels:
  - conda-forge
  - defaults
dependencies:
  - python>=3.9
  - pandas
  - numpy
  - xarray
  - fsspec
  - s3fs
  - pyarrow
  - cfgrib
  - requests
  - python-dateutil
  - pip
  - pip:
      - shutil  # Note: shutil is a standard library; pip installs will ignore this